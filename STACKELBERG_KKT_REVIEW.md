# 你提供的 KKT 推导核对

2026-09-10。核对依据是对话中贴出的推导和本目录 `FINAL_GUIDE.md`；你提到的 Windows 路径/更新 zip 没有出现在服务器目录中，未声称检查过其内容。

## 结论

客户端分段响应、内部参与门槛、最低支付、服务器梯度、固定模式的凸二次形式、KKT、正定情形的对偶公式均正确。预算乘子二分时必须重新求解耦合盒约束子问题，不能截断无约束矩阵解；你已经正确指出这一点。全局博弈仍需比较全部参与模式。

本地实现使用 SciPy SLSQP 求各凸子问题，并用独立支持超平面/对偶下界衡量全局数值间隙，并非你文中所述的主动集二分实现。`baqp/check_kkt.py` 独立核验你的解析 KKT 例子，`tests/test_mechanism.py` 另做价格空间网格对照。SLSQP 的 success 不作为全局最优证明；以报告的 `normalized_absolute_gap` 为准。

## 应写明的条件和尺度

1. **目标归一化。** 设共同 batch 噪声界为 v0，原目标为

   J_label = C_B G_cls² B_label + C_H G_cls² V_label + C_L v0 + C_A v0 Σa_k²。

   你的目标 J_short = B_label + κ_H V_label + κ_N Σa_k²，其中

   κ_H = C_H/C_B，κ_N = C_A v0/(C_B G_cls²)。

   两者满足 J_label/(C_B G_cls²) = J_short + C_L v0/(C_B G_cls²)。去掉这一项要求 v0 对所有参与配置相同；batch/方差不同或残差分支不能直接套用。代码 `objective_normalized = J_label/C_B`，因此

   objective_normalized = G_cls² J_short + C_L v0/C_B。

   预算约束相同，代码尺度的预算乘子也是你文中乘子的 G_cls² 倍。原未归一化目标的乘子是 C_B G_cls² 倍。

2. **凸性。** κ_H ≥ 0 即可，不要求 κ_H ≤ 1。H 可写为偏差 PSD 矩阵加 κ_H 倍加权方差 PSD 矩阵。实际协议计算的 κ_H 非负。

3. **固定参与模式。** 只有在该模式内，a_k 和噪声项才固定。不能对连续 q 求导后，忽略改变参与集合带来的归一化权重变化。原始参与、连续分支、完整增强的价格端点仍须分别处理。

4. **Slater 和退化情况。** 先消去固定变量。在非退化盒中，预算大于最低支付时可取靠近下界的严格可行点；预算等于最低支付时，由正成本参数下支付严格递增，可行域只剩下界点。预算小于最低支付则该模式不可行。

5. **奇异完整对偶。** 你给出的逆矩阵公式在 M = Q + νD 正定时正确。完整对偶域还可能包括 ν=0、Q 奇异的边界：若 z 属于 Range(M)，使用 Moore–Penrose 逆，二次项为 −zᵀM†z/4；否则下确界为 −∞。仅写 M 正定的公式可以作为分支公式，不能排除 ν=0 的最优乘子。

6. **一维对偶导数。** 只要盒约束内的极小解唯一即可使用所写导数，并不要求解严格位于盒内部。ν>0 时 D 正定保证唯一。ν=0 非唯一时需按超梯度处理，不能无条件宣称处处可微。

7. **λ_k = 0。** 不用 Λ⁻¹；该客户端固定 q_k=0，最低价格 s_k。你开头已经限定 λ_k>0，一般算法应保留这个特殊分支，代码已处理。

## 数值复核

可取真实整数标签计数 `[[140,139,121],[127,128,145]]` 重建你给定的 e、λ 和 E0；成本按你的例子覆盖。训练协议为 μ=0.05、L=0.55、η=0.1/L、h=2、batch=32、类别数3。

| 数值 | 独立复算 |
|---|---:|
| λ | 0.0848528137423857 |
| E0 | 0.001353125 |
| κ_H | 0.010045662100456623 |
| κ_N | 0.00004734848484848486 |
| 两客户端 u | 0.5704025757937787 |
| q | 0.0484002635220066 |
| 单价 r | 0.1804885666746314 |
| 各客户端支付 | 0.055 |
| 各客户端净效用 | 0.0144719806815467 |
| 简化目标 J | 0.0000261828909659418 |
| 预算乘子 ν | 0.0000391703333871180 |
| 单客户端最优 u | 0.8764870422406545 |
| 单客户端最优 J | 0.0000679910166235536 |
| 解析驻点无穷范数残差 | 约 3.05e-20 |
| 解析原始/对偶目标差绝对值 | 约 2.03e-20 |

解析闭式解的机器精度残差，与一般枚举求解器的数值间隙是两项不同检查。完整输出在 `outputs/kkt_review/example.json`。原始参与不可实施，完整增强单人最低支付为 0.1375>B=0.11，因此两个单人连续模式和双人连续模式覆盖本例全部非空可行情况。

## 对真实 AIGC 实验的关键限制

以上验证了博弈求解，不等于证明 test accuracy 提升。真实合成池定义 F_A，一般 F_A≠F。令 δ_A(w)=∇F_A(w)−∇F(w)，则实际混合训练满足

∇F_k(w;u_k)−∇F(w) = (1−u_k)Δ_k(w) + u_k δ_A(w)。

后一个生成分布偏差项不会由减小标签偏差自动消失。只修正原始客户端的有限样本残差，也没有包含该项。

如果有统一界 ε_A≥sup||δ_A||，结合 Δ_k=M e_k+r_k、||r_k||≤ε_k，可构造保守界：

B_actual² ≤ 3G_cls² B_label + 3(Σa_kρ_kε_k)² + 3(Σa_ku_k)² ε_A²；

H_actual² ≤ 3G_cls² V_label + 3Σa_kρ_k²ε_k² + 3Var_a(u) ε_A²。

这来自三项平方范数界和加权方差恒等式，固定模式内仍是凸二次上界；零残差的已知特例应使用更紧的原公式。当前代码没有把这组扩展界当作已认证生成误差模型。当前真实图像运行标记为代理目标实验，并保存训练集类别均值差作为诊断；该诊断不是 ε_A 的证明。

CNN 不满足文档的强凸性条件；其 L=.5+μ、G_cls²=2C、噪声界2/b仅沿用 softmax 的代理尺度，不是 CNN 的认证常数。多种子 test accuracy 配对差和消融结果才是本阶段“是否有效”的实证依据。
